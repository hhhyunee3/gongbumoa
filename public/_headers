# Cloudflare Workers 정적 자산 헤더 규칙
#
# 기본값이 "public, max-age=0, must-revalidate" 라서 재방문마다 서버에
# 재검증 요청이 오간다. 사진은 파일명이 1.jpg~42.jpg 로 고정이고 교체가
# 드물어, 30일 캐시로 왕복을 줄인다.
#
# 사진을 교체했는데 바로 반영해야 한다면 파일명을 바꿔서 올리면 된다.

/images/*
  Cache-Control: public, max-age=2592000
