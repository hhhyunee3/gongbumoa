name = "gongbumoa"
main = "gongbumoa_worker.js"
compatibility_date = "2026-07-23"

# image 폴더의 파일들(로고, 사진 등)을 정적으로 서빙합니다.
[assets]
directory = "./image"
binding = "ASSETS"

# 도메인을 사면 아래 주석을 풀고 도메인을 넣으세요.
# routes = [
#   { pattern = "gongbumoa.com", custom_domain = true }
# ]
